{
  "name": "gpt-pwa-supervisor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "concept:guard": "node scripts/concept-guard.mjs",
    "build": "tsc -b && vite build",
    "bundle:budget": "node scripts/check-bundle-size.mjs",
    "check": "npm run concept:guard && npm run build && npm run bundle:budget",
    "ci:local": "node scripts/local-ci.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.4.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
